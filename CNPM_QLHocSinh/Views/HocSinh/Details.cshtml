@model CNPM_QLHocSinh.Models.HocSinh

@{
    ViewBag.Title = "Học Sinh";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-body">
                    @Html.Partial("PartialViews/_HocSinhDetails", Model)

                    <div class="text-center mt-4">
                        @Html.Partial("_EditBtn", new { id = Model.MaHS })
                        @Html.Partial("_DeleteBtn", new { id = Model.MaHS })
                        @Html.Partial("_BackBtn")
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
