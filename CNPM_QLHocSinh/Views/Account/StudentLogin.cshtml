@model CNPM_QLHocSinh.Models.ViewModels.StudentLoginViewModel

@{
    ViewBag.Title = "Student Login";
}

<style>
    #div-containt {
        text-align: -webkit-center;
    }
</style>
<h2>Student Login</h2>

@using (Html.BeginForm("StudentLogin", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()

    <div class="form-group" style="padding-bottom:20px">
        @Html.LabelFor(m => m.MaHS, new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.MaHS, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.MaHS, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group" style="padding-bottom:20px">
        @Html.LabelFor(m => m.Password, new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Login" class="btn btn-success" />
        </div>
    </div>
}
