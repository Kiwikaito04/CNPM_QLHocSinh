@model IEnumerable<CNPM_QLHocSinh.Models.KhoiLop>

@{
    ViewBag.Title = "XemKhoiLop";
}

<h2>XemKhoiLop</h2>

<p>
    @Html.ActionLink("Tạo khối mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Mã khối lớp
        </th>
        <th>
            Tên khối lớp
        </th>
        <th>
            Mô tả
        </th>
        <th>
            Cấp bậc
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaKL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenKL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MoTa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CapBac)
        </td>
        <td>
            @Html.ActionLink("Chỉnh sửa", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Xoá khối", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
